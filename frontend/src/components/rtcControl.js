rootdir and refresh when build new connection
lscurrent
enterdir(加入返回上一级)
downloadfile
previewfile

考虑异步


File

ls + abs path ***

cd needs to be in download

download ***

preview ***


inuploadfile
op the file with the path

returndata:

ls currentdir
download***
preview**